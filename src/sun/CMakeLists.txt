# file(GLOB CPP [^.]*.cpp)
# file(GLOB HPP [^.]*.hpp)

# GLOBAL_ADD_INCLUDE(moon ${HPP})
# GLOBAL_ADD_SRC(moon ${CPP})

# message("*****************$ENV{GLOBAL_LIBRARY_SRCS}")

# add_library(asdf STATIC basic.cpp)
# target_include_directories(asdf PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})